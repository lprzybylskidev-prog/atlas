# Phase 25 — Admin panel rebuild and operational UX repair

**Status:** `complete`

## Objective

Rebuild the Admin panel views from the existing backend foundations and shared frontend primitives so Admin becomes a coherent operational tool instead of a collection of mixed, duplicated, partially localized, and noisy screens.

This phase is also the first full implementation of the future Atlas application view standard. The goal is not to build a separate Admin-only UI style, but to establish the same view contracts, shared primitives, interaction rules, data controls, localization discipline, review data, accessibility expectations, and testing expectations that later regular application screens must reuse.

This phase is the single active Admin repair scope.

## Dependencies

- [Phase 9 — Shared UI components](phase-09-shared-ui.md)
- [Phase 10 — Shared tables and saved views](phase-10-shared-tables-saved-views.md)
- [Phase 12 — Settings and localization](phase-12-settings-localization.md)
- [Phase 15 — Notifications and realtime foundation](phase-15-notifications-realtime.md)
- [Phase 16 — Admin operations and health](phase-16-admin-health.md)
- [Phase 20b — Managed processes, process logs, and scheduler](phase-20b-managed-processes.md)
- [Phase 21 — Imports](phase-21-imports.md)
- [Phase 22 — Search](phase-22-search.md)
- [Phase 23 — Feature flags](phase-23-feature-flags.md)
- [Phase 24a — Core export foundation and Admin data integration](phase-24a-core-export-foundation.md)
- [Admin module documentation](../modules/admin.md)
- [Notifications module documentation](../modules/notifications.md)
- [Settings module documentation](../modules/settings.md)
- [Frontend and shared UI architecture](../architecture/frontend-ui.md)
- [Tables, reports, exports, and print](../architecture/tables-reports-exports-and-print.md)
- [Quality gates and git workflow](../operations/quality-gates-and-git.md)

## Related documentation

- Module: `../modules/admin.md`
- Module: `../modules/notifications.md`
- Module: `../modules/settings.md`
- Architecture: `../architecture/frontend-ui.md`
- Architecture: `../architecture/tables-reports-exports-and-print.md`
- Operations: `../operations/quality-gates-and-git.md`

## Implementation contract

- The existing Admin backend foundations, domain/application logic, permissions, policies, route contracts, shared UI primitives, DataTable foundation, forms, dialogs, toasts, notifications, localization, and export infrastructure should be preserved where they are sound.
- Phase 25 must treat Admin as the first production-grade Atlas application surface, not as a special visual exception. Rebuilt Admin screens must create or prove patterns that can be reused by later non-Admin application views.
- The current Admin views must be treated as disposable. Rebuild the Admin Vue pages and Admin dashboard composition intentionally instead of patching low-quality screens with more explanatory copy.
- In Phase 25, "clean Admin views" means actually clearing/removing the existing Admin view implementations and rebuilding them from scratch on top of the accepted backend and shared UI foundations. It does not mean incremental styling, copy edits, or local patching of the current screens.
- Clearing Admin views does not mean discarding accepted frontend foundations. Rebuilt views must use the existing shared frontend modules, UI primitives, composables, services, formatters, table foundation, form primitives, modal/toast infrastructure, layouts, theme system, localization helper, route helpers, and test fixtures wherever they fit the accepted contracts.
- Existing Admin page code may be inspected only to recover required contracts, routes, props, permissions, actions, data ownership, and edge cases. It must not be treated as the design baseline for the rebuilt views.
- New frontend modules or primitives may be introduced only when the existing accepted frontend modules cannot express the rebuilt workflow cleanly; reusable additions must live in the shared frontend layer rather than inside one-off Admin pages.
- Any reusable frontend primitive, formatter, data-control pattern, interaction pattern, review-data convention, or browser-test convention introduced during this phase must be documented and shaped as an Atlas-wide pattern unless it is genuinely bound to Admin permissions or operational diagnostics.
- Repeated operational workflows must be rebuilt as coherent workflow modules, not recreated by hand from smaller primitives inside each page. If create/edit/show or two sides of one relation expose the same workflow, the shared workflow module must own layout, control sequence, labels, empty states, previews, action placement, and mode-specific rendering while pages provide route/data/permission/submit contracts.
- Before accepting a rebuilt sidebar entry, inspect the rebuilt pages for duplicated page-local workflow composition, page-local action factories, page-local option/formatter mappings, and page-local preview blocks. Anything shared by paired surfaces must either be extracted into a reusable workflow module/composable/formatter or explicitly documented as intentionally different.
- Do not remove backend capabilities merely because the current view is poor. Replace the view and presentation contract while preserving useful tested backend behavior.
- Admin pages must be operational tools, not documentation pages. If a view needs a long explanation to justify why it is useful, redesign the view with proper pagination, filters, drill-down, ownership, and actions.
- Remove the bounded-view-disclaimer pattern as a primary UI mechanism. If data is bounded, the view must expose a proper table, pagination, filters, date/range controls, counters, or a subtle scoped affordance. Long warning/explanation blocks are allowed only for genuinely exceptional or dangerous conditions.
- Admin dashboard must show concise, actionable operational summaries. It must not duplicate sidebar navigation, raw process steps, raw failed jobs, low-level event streams, or architecture explanations.
- Admin dashboard operational signals must be correctly attributed to their owner or rendered as global signals. Queue failures, managed-process failures, health/readiness failures, scheduler issues, operational alerts, module activation problems, audit/security findings, integration failures, file scan failures, search rebuild failures, and export/import failures must not appear under unrelated module cards.
- Queues and Managed Processes must have clear boundaries. Queues own low-level transport/job diagnostics. Managed Processes own Atlas workflow execution. When a queue failure belongs to a managed process, the richer workflow view must be the primary owning view and secondary views must cross-link instead of duplicating the problem as unrelated.
- The same asynchronous-work failure must not appear as independent unrelated errors in Dashboard, Queues, Managed Processes, Notifications, and module screens. It needs one owner, deduped summaries, and useful source deep links.
- Administrators must be able to mark owner-specific persistent operational problems as handled where the data owner can safely define that lifecycle, such as failed queue jobs, failed/warning/cancelled/expired managed-process runs, and problematic file scan states. Handled problems must stop shouting on dashboards and alerts unless the owning workflow reopens them through a new attempt, retry, rescan, worsening state, or explicit escalation rule.
- Operational handling actions must be backend-authorized, audited, team/module scoped where applicable, concurrency-safe, and localized.
- Notifications and toasts are not operational incident storage. They may inform about important state changes, but the owning Admin operational model owns handling, deduplication, reopening conditions, and dashboard visibility.
- Exporting from any Admin DataTable must produce at most one immediate request outcome toast/flash and one terminal notification. Managed-process step/status events such as running, validation, generation, storage, generated, or failed must not create visible toast stacks.
- Admin UI localization is complete only when rendered screens are reviewed in the browser and user-facing titles, subtitles, section labels, descriptions, card labels, table labels, empty states, action labels, dialog copy, validation messages, flashes, toasts, notifications, breadcrumbs, and helper text follow the active Polish/English locale.
- Technical diagnostic values may remain English where they are genuine operational identifiers, such as route names, permission names, queue names, job classes, process keys, exception classes, module keys, public identifiers, and correlation IDs. They must not be used as ordinary user-facing copy.
- Laravel translation files remain the canonical source of truth for Atlas-owned copy. Frontend views must receive translated text or a Laravel-derived active-locale dictionary and must not reintroduce independent manual PL/EN frontend catalogs.
- Every rebuilt Admin view must define its view contract before implementation: route name, Vue page, backend controller/provider, permission, module gate, active-team behavior, data ownership, primary actions, table/export behavior, dashboard signal behavior, breadcrumb, localization keys, manual review URL, and tests.
- Phase 25 must be implemented sidebar link by sidebar link. For each Admin sidebar entry, rebuild the full linked workflow before moving to the next entry: index/list, create, edit, show/detail, dialogs, row actions, bulk actions, filters, exports, empty/error/loading states, breadcrumbs, permissions, module gates, notifications/toasts, and all related subviews.
- After each sidebar entry is rebuilt, pause for owner browser review and explicit acceptance of that entry before continuing to the next Admin sidebar entry.
- Owner review feedback for a rebuilt sidebar entry becomes a reusable Atlas frontend guardrail, not a one-off fix. If the owner rejects or questions a pattern in `Users`, the same defect class must be searched for and prevented before rebuilding or accepting `Roles`, `Teams`, any later Admin entry, and any future regular App view.
- Repeated frontend defect classes discovered during this phase, such as dates bypassing shared formatters, raw technical values rendered as user copy, local table/filter/form styling, duplicated feedback messages, untranslated labels, weak empty states, or action placement mistakes, must be converted into explicit shared rules, shared primitives/formatters, tests, or review checklist items.
- Maintain a Phase 25 view-review findings log in this phase file while the phase is active. Each accepted owner finding must record the affected view, the issue, the accepted correction, whether it is local, Phase-25-wide, or Atlas-wide, and which existing and future views must be checked for the same defect class.
- Before starting and before accepting each subsequent sidebar entry, review the Phase 25 findings log and explicitly apply all Phase-25-wide and Atlas-wide corrections from earlier reviewed entries.
- When a finding is Atlas-wide, update the canonical shared frontend rules or architecture documentation before the phase is closed so future non-Admin application views cannot reintroduce the same class of problem.
- Before Phase 25 is closed, update `AGENTS.md` with the permanent repository-wide frontend working rules proven by this phase so future agents cannot repeat the same view-quality, localization, formatting, feedback, review-data, or acceptance-process failures.
- During this phase, every rebuilt Admin sidebar entry must receive deterministic review/demo data that exposes the full practical range of states, options, permissions, module states, edge cases, empty states, validation errors, operational failures, and action paths for that entry.
- After each Admin sidebar entry is accepted, the database must still contain a useful representative dataset for that entry so the owner can keep reviewing the complete cross-section of available options while later entries are rebuilt.
- During Phase 25, all review-only seeders, helper classes, generated data, temporary routes, temporary UI controls, and temporary test harnesses required for owner review must remain available until the owner gives final acceptance for the whole phase.
- After final owner acceptance of Phase 25, perform a cleanup pass that removes all Phase 25 review-only code, seeders, fixtures, generated demo data, temporary routes, temporary UI controls, and temporary test harnesses. The owner explicitly rejected keeping the Phase 25 review dataset as permanent demo scope on 2026-07-31. The repository must return to the clean accepted state: no review-only code, no `app/Modules/Application/Demo`, and an empty/no-op `Database\Seeders\DevelopmentDemoSeeder`.
- Rebuilt Admin views must compose existing shared primitives first. New primitives may be added only when they remove real duplication and become part of the shared UI layer.
- Rebuilt Admin views must avoid Admin-only visual language when the pattern is generally useful. Tables, forms, filters, dialogs, detail views, empty states, action bars, validation summaries, status badges, pagination, exports, and review workflows should be designed as application-level Atlas patterns and only composed differently where Admin-specific authorization or operational ownership requires it.
- Rebuilt views must support light/dark themes, keyboard interaction, accessible labels, clean browser console, deterministic query-string state where applicable, and no overlapping text.
- Admin implementation must include meaningful browser-level verification. Backend feature tests alone are insufficient for this phase.

## Scope

Rebuild or replace the current Admin presentation for at least:

- Admin dashboard and operational summary;
- system status, health/readiness, release metadata, scheduler, operational alerts;
- queues and failed job diagnostics;
- managed processes, process details, imports, schedules, and definitions;
- files and file scan operations;
- integrations and sync runs;
- search and rebuild runs;
- feature flags and flag history;
- modules, module activation, and activation history;
- audit browser and security history;
- users, teams, manager hierarchy, roles, permissions, and onboarding packages;
- rate limits;
- notifications center/dropdown where it interacts with Admin operational workflows;
- Admin DataTable export actions and terminal notification behavior.

## Known findings to repair

The following findings must remain in scope and cannot be dropped during rebuild:

- Admin dashboard after demo reset showed most content in English while navigation links were Polish.
- Admin panel contained mixed-language user-facing copy across dashboard, operational cards, backend-provided props, and frontend page text.
- Laravel-owned translations must remain canonical; frontend must not maintain a second manual translation catalog.
- Natural-language keys must not be used for Atlas-owned copy, except Laravel/framework/vendor source-string compatibility entries.
- Export actions created toast storms with managed-process step messages such as `Failed`, `Running`, `Snapshot validated`, `Generating report export`, `Storing generated artifact`, and `Report export generated`.
- Toast bodies exposed raw technical values such as `managed_process`.
- Admin dashboard appeared to show queue failures under a `core Authorization` module/status block, suggesting broken signal attribution.
- Queues and Managed Processes presented overlapping async failure information while one view was poorer and the dashboard kept warning without an operator lifecycle.
- Admin views used large bounded-view notices such as "newest 80 runs", "newest 20 syncs", "newest 50 flag history", and similar explanatory copy instead of good pagination/filter/drill-down UI.
- Admin dashboard and operational screens exposed architecture explanations rather than concise actionable operator state.
- Operational failures had no acknowledge/resolve/reopen lifecycle, so handled problems could keep shouting indefinitely.
- Admin dashboard signal ownership, deduplication, and source deep-linking were unclear.
- Existing automated tests were too weak to catch broken rendered Admin localization, toast storms, and poor operational UX.

## Tasks

- [x] Make this phase the active Admin repair phase in `WORKROAD.md`.
- [x] Inventory all current Admin Vue pages, Admin route contracts, controllers/providers, shared primitives, permissions, module gates, breadcrumbs, dashboard signals, tables, exports, notifications, and tests.
- [x] Identify backend foundations that should be preserved and presentation code that should be deleted or replaced.
- [x] Define the Atlas-wide view standard proven by this Admin rebuild: page structure, table/filter/form/detail/action patterns, state handling, localization, review data, acceptance flow, and browser-test expectations for future App screens.
- [x] Clear/remove the current Admin view implementations and rebuild them from scratch on top of accepted backend contracts and shared UI primitives.
- [x] Reuse accepted shared frontend modules, UI primitives, composables, services, formatters, table/form/dialog/toast infrastructure, layouts, theme system, localization helper, and route helpers while rebuilding each Admin view.
- [x] Use existing Admin page code only as an inventory source for routes, props, permissions, actions, edge cases, and data ownership; do not reuse it as the rebuilt design baseline.
- [x] Design the rebuilt Admin information architecture: dashboard, identity/access, organization, oversight, operations, diagnostics, and system configuration groupings.
- [x] Define the sidebar-link rebuild order and an acceptance checklist that must be completed for each entry before work continues to the next entry.
- [x] Create and maintain the Phase 25 view-review findings log so owner feedback from each rebuilt view becomes a reusable checklist for all later Admin and future App views.
- [x] Build deterministic Admin review/demo seed data for each sidebar entry that exposes the full practical range of options, states, permissions, module states, operational failures, empty states, validation paths, and action paths for that entry.
- [x] For each Admin sidebar entry, rebuild and review the complete workflow before moving on: index/list, create, edit, show/detail, dialogs, filters, row/bulk actions, exports, empty/error/loading states, breadcrumbs, permissions, module gates, toasts/notifications, and subviews.
- [x] After each sidebar entry, run focused backend/frontend tests and owner browser review, record acceptance, record any owner findings, apply accepted Atlas-wide findings to the reusable checklist, and preserve a representative seeded dataset for that accepted entry before proceeding to the next entry.
- [x] Before accepting every sidebar entry after the first one, verify that all earlier Phase-25-wide and Atlas-wide owner findings have been applied and the same class of defect has not reappeared.
- [x] Convert repeated defect classes into shared primitives, formatters, lint/test guardrails, or canonical frontend documentation instead of relying on memory.
- [x] Define the Admin dashboard signal model, ownership, severity, deduplication, source deep links, and global-versus-owned rendering.
- [x] Implement owner-specific operational handling persistence and services for failed queue jobs, managed-process runs, and problematic file scan states, including source identity, owner, reason, actor, and timestamps where applicable.
- [x] Add authorization, audit, validation, concurrency-safe writes, and localization for the accepted owner-specific handling actions.
- [x] Rebuild Admin dashboard as a concise actionable operational overview using correctly attributed global/owned signals and handled-state exclusions.
- [x] Rebuild Queues as low-level transport diagnostics with clear links to managed-process/workflow owners when applicable.
- [x] Rebuild Managed Processes as the primary Atlas workflow execution view with proper filters, pagination, detail logs, retry/cancel actions, and links to related queue/export/import/file/search records.
- [x] Rebuild Files, Search, Integrations, Feature Flags, Modules, Audit/Security, Rate Limits, Application Logs, and Notifications Admin views with proper data ownership, filters, pagination, empty states, actions, exports, and localized copy. Import operational visibility is accepted as part of Managed Processes, not as a separate Admin route.
- [x] Restore or intentionally replace Admin diagnostic navigation links for package-owned Pulse and Telescope surfaces, including route visibility, permissions, environment constraints, and clear separation from rebuilt Atlas-owned Inertia views.
- [x] Rebuild Users, Teams, Managers, Roles, Permissions, and Onboarding Packages views only as needed to meet the same Admin UX, localization, table, form, validation, and action standards.
- [x] Before accepting the rebuilt Users entry, replace the page-local create/edit Team access sections with one shared workflow module that supports create and edit modes. The module must own the add-team control, team row/card layout, assignment source selector, preset/copy/manual controls, roles/direct-permissions controls, effective preview, remove/save action placement, placeholders, empty state, disabled states, and localization hooks.
- [x] Before accepting the rebuilt Users entry, audit and extract other repeated Users presentation contracts where appropriate: account action definitions shared by index/edit, account sensitivity labels/options shared by table/edit/export-adjacent UI, and any remaining duplicated authorization preview or team-assignment mapping. Keep page files limited to route/data/permission/submit wiring and workflow-module composition.
- [x] Remove bounded-view warning blocks and replace them with proper data controls or subtle scoped metadata.
- [x] Ensure every export action emits at most one immediate request outcome and one terminal notification, with no managed-process toast storm.
- [x] Audit all Admin flash/toast producers so one user action emits at most one immediate flash/toast, no flash/toast uses raw technical wording, and technical details stay in tables, detail screens, logs, audit records, or process logs.
- [x] Ensure notifications, toast viewport, realtime reloads, and Admin dashboard summaries do not duplicate the same operational state.
- [x] Move all Atlas-owned Admin copy into Laravel-owned translation files and verify PL/EN rendered UI completeness.
- [x] Add backend feature tests for rebuilt Admin route contracts, Inertia props, authorization, validation, operational handling lifecycle, signal attribution, deduplication, and export message ownership.
- [x] Add Vitest coverage for Admin UI services/composables, localization helper use, toast/notification behavior, dashboard signal mapping, table state, and shared primitives introduced or changed by this phase.
- [x] Add Playwright coverage for the rebuilt Admin dashboard, major Admin areas, Polish/English rendered localization, no accidental English user-facing copy in Polish mode, no export toast storm, acknowledge/resolve/reopen, source deep links, light/dark themes, and clean browser console.
- [x] Run `composer demo:reset`, `pnpm build`, and browser-review every rebuilt Admin area in Polish and English before marking localization complete.
- [x] Keep all Phase 25 review-only seeders, fixtures, helper classes, generated review data, temporary routes, temporary UI controls, and temporary test harnesses available until the owner gives final acceptance for the whole phase.
- [x] After final owner acceptance, remove all Phase 25 review-only code, seeders, fixtures, generated review data, temporary routes, temporary UI controls, and temporary test harnesses, returning the repository to a clean state with no `app/Modules/Application/Demo` and an empty/no-op development demo seeder.
- [x] Search for and remove legacy Admin routes, controllers, breadcrumbs, navigation entries, permissions, translation keys, tests, and Vue pages left behind by old views that were replaced or absorbed by accepted Phase 25 workflows.
- [x] Update Admin, Notifications, Settings, Operations, Frontend UI, testing, and roadmap documentation.
- [x] Update shared Frontend UI documentation so reusable patterns created during this Admin rebuild become the baseline for later regular application views.
- [x] Update `AGENTS.md` with permanent Atlas-wide frontend rules proven by Phase 25, including view-by-view acceptance, reusable findings, formatter/localization requirements, review-data expectations, toast/notification discipline, and shared primitive usage.
- [x] Run relevant quality gates.
- [x] Commit Admin panel rebuild and operational UX repair.

## Completion criteria

- [x] Existing poor Admin views have been removed or replaced with coherent rebuilt views.
- [x] Admin dashboard is a concise actionable operational overview with correct signal ownership, deduplication, source links, and owner-specific handling lifecycle behavior.
- [x] Queues and Managed Processes have clear boundaries and no longer present the same async failure as unrelated duplicated errors.
- [x] Administrators can mark accepted owner-specific operational issues as handled; handled issues stop warning unless they recur, worsen, or escalate through the owning workflow.
- [x] Admin views no longer rely on large bounded-view disclaimer blocks to compensate for weak data controls.
- [x] Admin DataTable exports no longer create managed-process toast storms.
- [x] Admin rendered UI is complete in Polish and English except for allowed technical diagnostic values.
- [x] Laravel translations remain canonical and no independent frontend PL/EN catalog returns.
- [x] Rebuilt views are accessible, responsive, theme-complete, keyboard-usable, and free from obvious text overlap.
- [x] Phase 25 leaves behind an Atlas-wide view standard that future regular application screens can reuse instead of inventing separate App-only patterns.
- [x] Backend, Vitest, and Playwright regression coverage protects the rebuilt Admin UX and operational behavior.
- [x] Every Admin sidebar entry was rebuilt as a complete workflow, reviewed in the browser by the owner, and explicitly accepted before moving to the next entry.
- [x] Owner review findings from earlier sidebar entries were carried forward and verified against later entries, so the same accepted defect class does not repeat across the Admin rebuild or future App view standard.
- [x] Repeated frontend defect classes discovered during Phase 25 have been converted into shared implementation guardrails, tests, or canonical documentation.
- [x] `AGENTS.md` contains the final Phase 25 frontend guardrails so future agents do not need to rediscover the same process through repeated failed Admin/App views.
- [x] During owner review, the Phase 25 review dataset shows a full practical cross-section of every rebuilt Admin sidebar entry.
- [x] After final owner acceptance, all Phase 25 review-only scaffolding, helper classes, generated review data, temporary routes, temporary UI controls, and temporary test harnesses have been removed.
- [x] After final owner acceptance, `app/Modules/Application/Demo` has been removed and the development demo seeder is empty/no-op again.
- [x] Canonical documentation is current and Phase 26 can begin safely.

## Phase 25 View-Review Findings Log

Accepted owner findings recorded during sidebar-by-sidebar rebuild must be added here while this phase is active. These findings are binding for later rebuilt views in Phase 25, and Atlas-wide findings must be carried into shared frontend documentation, `AGENTS.md`, tests, primitives, or formatters before the phase is closed.

| Date | View | Finding | Accepted correction | Scope | Existing/future views to check | Guardrail update | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-23 | Users | Row and record actions were conditionally hidden when the current record state made them inapplicable, leaving administrators without a stable full action set. | Rebuilt Admin views must keep the full authorized action set visible and use disabled states with localized reasons for state-inapplicable actions instead of removing actions from the UI. | Phase-25-wide | Users index/edit, then Roles, Teams, Managers, Permissions, Onboarding Packages, and every later Admin action surface. | Shared `DataTable` and `RecordActions` now support disabled actions and localized disabled reasons. | Applied to Users; carry forward. |
| 2026-07-23 | Users create/edit | Selects with empty values behaved inconsistently because some flows used a synthetic empty option while others used the shared select placeholder. The Team access workflow also used different card/list composition and add flows between create and edit. | Empty select values must render through the shared `FormSelect` placeholder model, not synthetic empty options. Create/edit variants of one workflow must share the same select-then-action card/list structure unless the task genuinely differs. | Phase-25-wide | Users create/edit, then Teams create/edit, Roles create/edit, Onboarding Packages create/edit, and every later form with nullable/select-first controls. | Users create Team access now follows the edit-style select team -> add access -> configure access structure and placeholder-only empty select behavior. | Applied to Users; carry forward. |
| 2026-07-23 | Users edit/team access/navigation | The team-access add button label described the resulting access instead of the selected entity, edit showed a redundant active-team badge, edit lacked the effective authorization preview available during create, and Users sat inside the workspace navigation group. | Rebuilt paired create/edit authorization workflows must use the same semantic button labels, avoid redundant status badges in repeated rows, expose the same effective-permission preview where assignments can change, and place identity/access workflows in their own sidebar group. | Phase-25-wide | Users create/edit/navigation, then Teams, Roles, Managers, Permissions, Onboarding Packages, and every later Admin identity/access workflow. | Users navigation now has an Identity & access group; Users create/edit use Add team; Users edit removes the active-team row badge and adds effective authorization preview. | Applied to Users; carry forward. |
| 2026-07-23 | Users create/edit | Some form action buttons were text-only, team-access placeholders used stiff audit/process wording, and Admin-created accounts were not explicitly created as sensitive accounts. | Rebuilt operational forms must use icon-led action buttons, placeholders must sound like concrete operator input examples instead of internal process labels, and administrator-created accounts must be explicitly sensitive unless a later accepted workflow says otherwise. | Phase-25-wide | Users create/edit, first-administrator bootstrap, then Teams, Roles, Managers, Permissions, Onboarding Packages, and every later Admin form. | Users `FormButton` usage is guarded by a Vitest check; team-access placeholders are natural PL/EN copy; Admin UI and first-administrator bootstrap pass `sensitive` through the user creation contract. | Applied to Users; carry forward. |
| 2026-07-23 | Users edit | User edit exposed only manual role/direct-permission editing while create exposed manual, preset, and copy modes. Record actions were created once during setup, so their labels and disabled reasons did not react to in-app language changes until a full reload. | Paired create/edit workflows must expose the same practical assignment modes unless backend state makes a mode impossible. User-facing labels, action labels, and disabled reasons that depend on the active translation catalog must be computed/reactive rather than setup-time constants. | Phase-25-wide | Users edit, then Teams, Roles, Managers, Permissions, Onboarding Packages, and every later Admin form/action surface with in-app locale switching. | Users edit now receives packages/copy sources, supports manual/preset/copy assignment modes, and uses computed record actions; a Vitest guardrail checks these requirements. | Applied to Users; carry forward. |
| 2026-07-23 | Users create/edit Team access | Create and edit Team access were repeatedly patched as separate page-local compositions. Even when they exposed similar data and controls, their layout, control placement, action placement, and source-mode rendering drifted because no single workflow module owned the complete Team access experience. | Shared relation/assignment workflows must be implemented as one coherent workflow module with explicit create/edit modes before owner acceptance. Pages must not hand-compose the same workflow from `SurfaceCard`, form controls, preview blocks, and buttons in multiple places. | Phase-25-wide and Atlas-wide frontend rule | Users Team access immediately; then Teams user access, Roles/Permissions assignment surfaces, Onboarding Packages create/edit, Managers relationship editor, and later App workflows with paired create/edit surfaces. | `AGENTS.md` now requires coherent workflow modules for repeated operational workflows. Users Team access now uses one shared `UserTeamAccessWorkflow`; Users account actions and sensitivity labels/options use shared composables. | Applied in code; requires owner browser review. |
| 2026-07-23 | Users, Permissions, Roles, Szablony, and Teams index | Rebuilt DataTable screens exposed search, saved views, columns, and exports, but lacked explicit operational filters for common narrowing such as account state, MFA, module, assignment, ownership, active state, and effective permission state. | Admin DataTable screens that have meaningful discrete states or ownership dimensions must expose shared `FilterPanel` controls wired to backend filtering before pagination and export-compatible query keys. | Phase-25-wide | Users, Permissions, Roles, Teams, and Onboarding Packages immediately; then Managers, Audit, Queues, Processes, Modules, and every later Admin DataTable. | Users, Permissions, Roles, Szablony, and Teams now use shared `FilterPanel` controls; controllers apply filters before pagination and return `table.state.filters`; export providers keep filter-compatible query keys where exports are available; focused feature tests cover the filtered contracts. | Applied to rebuilt DataTable entries; carry forward. |
| 2026-07-23 | Roles index and Users Team access | Roles exposed the Spatie `team_id`/scope implementation detail even though Atlas defines role and permission catalogs globally and applies team context when assigning roles/permissions to a user. Users Team access also lacked the searchable, counted role/permission checklist proven in the role form. | Admin role catalog views must not expose role `team_id` or team/global scope controls unless a later accepted product contract introduces team-specific role definitions. Role/permission checklist surfaces must use searchable lists with selected counters. | Phase-25-wide | Roles immediately; Users Team access immediately; then Teams user access, Onboarding Packages role/permission presets, and every later authorization-assignment surface. | Roles table/export/controller contract no longer exposes `teamId` or role scope filters. `SearchableCheckboxList` is now shared by `RoleForm` and `UserTeamAccessWorkflow`. | Applied in code; requires focused owner browser review. |
| 2026-07-23 | Szablony, Roles, Permissions, Teams | The Polish Admin UI called onboarding assignment templates "Presety", and roles, permissions, and teams exposed technical `name` values as the primary visible label. Package create/edit also used a separate form composition even though it assigns the same role/direct-permission sets as user Team access. | Polish UI must call onboarding packages "Szablony"; roles, permissions, teams, and assignment options need a human display name separate from the stable technical name; template create/edit forms must use the same searchable counted assignment pattern and effective-permission preview used by user Team access. | Phase-25-wide | Szablony immediately; Roles, Permissions, Users Team access, Teams rebuild, exports, breadcrumbs, navigation, validation, and every later authorization-assignment surface. | Added persisted `display_name` fields for roles, permissions, and teams, display-name-aware table/export/selector contracts, Polish "Szablony" copy, and a shared `OnboardingPackageForm` assignment preview. | Applied in code; Teams visible pages still await their Phase 25 rebuild. |
| 2026-07-23 | Admin navigation | The Teams link existed in the desktop and mobile navigation components, but `HandleInertiaRequests` did not include `admin.teams.index` in `auth.availableAdminRoutes`, so the frontend hid the entry after demo reset/build even for authorized administrators. | Every rebuilt Admin sidebar entry must be registered in both the frontend navigation contract and the backend `availableAdminRoutes` payload, with a feature test proving the route appears for an administrator after seeded starter roles are installed. | Phase-25-wide | Zespoły immediately; then every later Admin sidebar entry as it is rebuilt. | Added `admin.teams.index` to `availableAdminRoutes` and extended `FrontendShellTest` to assert that an administrator receives the Teams route in the shared Inertia auth payload. | Applied to Zespoły; carry forward. |
| 2026-07-31 | Files navigation | The rebuilt Files page was reachable directly, but the sidebar link did not appear because `admin.files.index` was missing from `auth.availableAdminRoutes` and from the desktop/mobile navigation definitions. | Every rebuilt Admin sidebar entry must be added to `HandleInertiaRequests::availableAdminRoutes`, desktop `Sidebar`, mobile navigation, and a shell feature assertion before the entry is offered for owner review. | Phase-25-wide | Files immediately; Imports, Search, Integrations, Feature Flags, Audit/Security, Rate Limits, Notifications, and every later Admin sidebar entry. | Added `admin.files.index` to the shared Inertia route payload, desktop sidebar, mobile navigation, and `FrontendShellTest`. | Applied to Files; carry forward. |
| 2026-07-31 | Files operational handling | The scan-evidence subtitle repeated obvious table scope copy, and problematic file rows had rescan but no mark-as-handled lifecycle, so intentionally reviewed bad/stuck files would keep appearing as active work. | Operational Admin views that expose persistent failures or blockers need the same row/bulk handling affordance pattern as Queues and Managed Processes, plus default filters that hide handled rows while preserving diagnostics. Avoid explanatory helper copy when the view structure already communicates the scope. | Phase-25-wide | Files immediately; Imports, Search, Integrations, Feature Flags, Audit/Security, Rate Limits, Notifications, and every later operational Admin entry. | Removed the scan-evidence subtitle; added file handling persistence, backend authorization, security audit, row/bulk mark-as-handled actions, handling filter, exports, dashboard blocker exclusion, and rescan reopening. | Applied to Files; carry forward. |
| 2026-07-23 | Managers and Modules date inputs | Rebuilt pages used generic `FormInput type="date"` and `type="datetime-local"` even though shared `FormDateInput` and `FormDateTimeInput` already existed. The shared date primitives also contained hardcoded English month navigation/time labels. | Rebuilt Admin views must use dedicated date/time primitives for date and timestamp controls, and those primitives must be fully localized. Generic `FormInput` is not allowed for date or datetime entry outside the shared form primitive layer. | Phase-25-wide and Atlas-wide frontend rule | Managers immediately; Modules immediately; then Queues, Managed Processes, Imports, Search, Integrations, Feature Flags, Audit, Rate Limits, Notifications, and every later date/time form. | `formControls.test.ts` now rejects `FormInput type="date"` and `FormInput type="datetime-local"` outside `Components/Form`; Managers and Modules use `FormDateInput`/`FormDateTimeInput`; date picker navigation and time labels use Laravel translations. | Applied in code; carry forward. |
| 2026-07-30 | Modules detail | The module detail view used technical labels such as "effective" and "team source", showed duplicate "no global support/read-only" badges, and displayed activation/scheduling forms even for non-editable Core modules such as Audit. The selected-team control also appeared below state values that depend on it, empty schedule selects looked broken, and global module controls were mixed with per-team override controls in one screen. | Module activation UI must use operator-facing labels: active state and configuration source. The module detail screen must be module/global-focused. Team-specific activation, scheduling, and clearing controls must live in a separate team-configuration screen reached from the team table. Non-editable modules must render as information-only state/dependency/team tables without activation, scheduling, clearing, history, or schedule-management forms. Empty shared selects must render a useful disabled placeholder. Shared date/time primitives must tolerate partial date/time input without collapsing the calendar. | Phase-25-wide | Modules immediately; then Feature Flags, Teams module override surfaces, Managed Processes schedules, Imports/Search schedules, and every later Admin date/select/status surface. | `FormSelect` now supports disabled empty placeholder behavior; date/time primitives guard partial input; Modules index/detail copy and exports use Active and Configuration source; Module detail branches editable and non-editable modules; team override controls moved to `Admin/Modules/TeamConfiguration`. | Applied in code; pending owner browser review. |
| 2026-07-30 | Modules review data | Existing deployed modules were either non-editable Core modules or optional foundation modules with real operational meaning, making it awkward to demonstrate pure module activation semantics before business modules exist. Automatically enabling every globally configurable module also removed the useful "deployed but awaiting acceptance" state for future business modules. | Phase 25 may include a temporary registry-only Application module named `demo` that supports global and team activation, has no routes or business behavior, is not automatically enabled by bootstrap, and starts without global or team activation rows. Application-category modules may therefore be deployed in code while waiting for explicit administrative acceptance. The placeholder must be clearly documented as review-only and removed before real business-module work begins unless explicitly accepted as permanent demo scope. | Local to Phase 25 review data; Application module bootstrap rule | Modules immediately; Phase 25 cleanup and first Application module implementation. | Added `app/Modules/Application/Demo`, registered it explicitly, skipped Application-category modules in system bootstrap activation, documented the placeholder, and covered that it appears in Admin module activation as globally disabled with no team override rows. | Applied in code; owner requested `test` -> `demo` naming for consistency. |
| 2026-07-30 | Managed Processes review | The separate Import executions subview split ordinary process runs and import process runs even though imports are managed process executions with additional import metadata. Schedule creation also lived inline in the schedules list, and empty selects were disabled instead of showing the same placeholder-only state as other empty controls. | Managed Processes must use one combined run list for ordinary and import-linked process runs. Import definitions belong in the same definition catalog. Schedule creation must be a separate page. Empty selects must remain enabled and show their placeholder unless explicitly disabled by the caller. | Phase-25-wide workflow composition and Atlas-wide form primitive behavior | Managed Processes immediately; all future select controls and operational run/import views. | Combined import fields into the run table contract, removed the old import subnavigation/page and stale import routes, added create pages for schedules and definition starts, added demo ordinary/import no-op definitions, and changed `FormSelect` so an empty options list does not implicitly disable the control. | Applied in code; pending owner browser review. |
| 2026-07-30 | Managed Processes long jobs | Long-running demo review exposed that `managed-processes` and `imports` queues were not part of the local worker queue list and that short worker timeouts do not match future debt-collection imports or API scripts that may run for hours. The owner accepted long single jobs as a supported operational model, with idempotency/checkpoint choices owned by the process implementation rather than forced by the platform. | Managed Processes and Imports must support long-running single queue jobs while still showing one process run in Admin. Worker timeout is 12 hours by default for the dev/Horizon worker contract, Redis `retry_after` must exceed that timeout, and process-specific resume/idempotency strategy remains module-owned. | Managed Processes and Imports operational contract | Managed Processes, Imports, production/development worker configuration, future API imports. | Added `managed-processes` and `imports` to the local worker/Horizon queue list, raised worker/Horizon timeout to 43,200 seconds, raised Redis `retry_after` default to 43,500 seconds, and documented that one visible process run may correspond to a long single queue job. | Applied in code; pending owner browser review. |

## Phase 25 Sidebar Entry Review Log

| Date       | Sidebar entry | Routes / pages                                                                                                                                                                                                                                                                                                                                 | Backend owner                                                                                                                                                                                                                                      | Review state                                                                                                                                        | Quality checks                                                                                                                                                                                                                                                                                                                                                                                                                                             | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-23 | System Status | `/admin`, `/admin/system-status/*`; `Admin/SystemStatus`                                                                                                                                                                                                                                                                                       | `AdminSystemStatusController`                                                                                                                                                                                                                      | Owner accepted as first rebuilt Admin view.                                                                                                         | Covered by prior Phase 25 dashboard work.                                                                                                                                                                                                                                                                                                                                                                                                                  | Keep dashboard patterns and owner feedback active for later entries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-23 | Users         | `/admin/users`, `/admin/users/create`, `/admin/users/{user}/edit`, account actions, team access actions; `Admin/Users/Index`, `Admin/Users/Create`, `Admin/Users/Edit`, shared `UserTeamAccessWorkflow`, Users account-action and sensitivity composables                                                                                      | Core Users, Teams, Authorization, Identity controllers and shared DataTable exports/saved views                                                                                                                                                    | Owner accepted by directing work to the next sidebar entry; filter and checklist ergonomics gaps found during Authorization review and retrofitted. | `pnpm typecheck`; focused `php artisan test` for Admin users table, user creation, first-administrator sensitivity, team access props, online status, and Users index filters; targeted ESLint/Vitest/build on rebuilt Users pages during review iterations.                                                                                                                                                                                               | Phase 25 working convention clarified: when the owner asks to proceed to the next view, the previous rebuilt view is accepted. Users now has explicit backend-applied filters plus searchable counted role/direct-permission lists in Team access.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-23 | Permissions   | `/admin/authorization/permissions`; `Admin/Authorization/Permissions`                                                                                                                                                                                                                                                                          | Core Authorization permission catalog, effective permission checker, module activation state, shared DataTable exports/saved views                                                                                                                 | Owner accepted; earlier review state was a documentation omission.                                                                                  | `php artisan test --filter='test_admin_(permissions_catalog_(is_readonly_table_with_effective_state\|applies_filters_before_pagination)\|users_table_applies_filters_before_pagination)'`; `pnpm typecheck`; `pnpm exec eslint ...`; `pnpm exec prettier --check ...`; `./vendor/bin/pint --dirty`.                                                                                                                                                        | Carry Users findings forward: stable readonly surface, reactive localization, no row/bulk actions, explicit filters, no visible English catalog descriptions in the default Polish table.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-23 | Roles         | `/admin/authorization/roles`, `/admin/authorization/roles/create`, `/admin/authorization/roles/{role}/edit`, store/update/delete; `Admin/Authorization/Roles`, `Roles/Create`, `Roles/Edit`, shared `RoleForm`                                                                                                                                 | Core Authorization role controllers, permission catalog, audit, high-risk Admin middleware, shared DataTable exports/saved views                                                                                                                   | Owner accepted; earlier review state was a documentation omission.                                                                                  | `php artisan test --filter='test_admin_roles_workflow_exposes_global_role_index_and_create_edit_contracts'`; included in focused Admin CRUD/filter test run; `pnpm typecheck`; `pnpm build`; focused ESLint/Prettier; focused Pint on touched PHP files.                                                                                                                                                                                                   | Role definitions are global in the Admin UI; Spatie role `team_id`/scope remains internal only. Index has backend-applied assignment and permission-count filters that remain export-compatible. Create/edit share `RoleForm`; delete remains visible but disabled with localized reason when a role is assigned to users.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-23 | Szablony      | `/admin/authorization/packages`, `/admin/authorization/packages/create`, `/admin/authorization/packages/{package}/edit`, store/update/deactivate; `Admin/Authorization/Packages`, `Packages/Create`, `Packages/Edit`, shared `OnboardingPackageForm`                                                                                           | Core Authorization onboarding package controllers, role catalog, permission catalog, active teams, high-risk Admin middleware, shared DataTable exports/saved views                                                                                | Owner accepted; earlier review state was a documentation omission.                                                                                  | `php artisan test --filter='test_admin_onboarding_presets_workflow_exposes_table_and_create_edit_contracts'`; included in focused Admin CRUD/filter test run; `pnpm typecheck`; `pnpm build`; focused ESLint/Prettier; focused Pint on touched PHP files.                                                                                                                                                                                                  | Szablony remain team-scoped one-time onboarding templates. Index has backend-applied status and team filters. Create/edit share one workflow form with team context, searchable counted role and permission lists, effective-permission preview, icon-led actions, localized breadcrumbs/navigation, and exportable DataTable index.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-23 | Zespoły       | `/admin/teams`, `/admin/teams/create`, `/admin/teams/{team}/edit`, activate/deactivate/delete, team-side member access actions; `Admin/Teams/Index`, `Admin/Teams/Create`, `Admin/Teams/Edit`, shared `TeamForm`, shared `TeamMemberAccessWorkflow`                                                                                            | Core Teams, Authorization, Identity account directory, module activation, audit, shared DataTable exports/saved views                                                                                                                              | Owner accepted; earlier review state was a documentation omission.                                                                                  | `php artisan test --filter='AdminAdministrationCrudTest'`; `pnpm typecheck`; `pnpm build`; focused ESLint/Prettier; focused Pint on touched PHP files.                                                                                                                                                                                                                                                                                                     | Zespoły now have localized routes/pages again, backend-applied active-status filters, member counts, stable row actions with disabled reasons, display-name-aware tables/exports/selectors, and a team-side member authorization workflow with effective-permission preview.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-23 | Managerowie   | `/admin/managers`, `/admin/managers/create`, `/admin/managers/{user}/edit`, store/end/head update actions; `Admin/Managers/Index`, `Admin/Managers/Create`, `Admin/Managers/Edit`, shared `ManagerHierarchyTree`, shared recursive `ManagerHierarchyTreeNode`                                                                                  | Core Teams manager hierarchy public contract, manager relationship history export provider, active-team authorization and Admin route visibility                                                                                                   | Owner accepted by closing the managers view and moving to Modules.                                                                                  | `php artisan test --filter='ManagerHierarchyAdministrationTest\|DemoResetTest\|FrontendShellTest'`; `pnpm typecheck`; `pnpm build`; focused ESLint/Prettier; `./vendor/bin/pint --dirty --test`.                                                                                                                                                                                                                                                           | Managerowie index is now a DataTable of manager users for the selected team with an Add manager action and filters for manager type plus direct/subtree report presence. Manager create mirrors the accepted edit control layout: selected manager context, hierarchy preview, bulk direct-report assignment with preview, direct-report row termination, and head-manager controls. The redundant edit history DataTable was removed after moving start date and creation reason into the direct-report rows.                                                                                                                                                                                                                                                 |
| 2026-07-23 | Moduły        | `/admin/modules`, `/admin/modules/{module}`, `/admin/modules/{module}/teams/create`, global/team activation actions, global/team scheduling, schedule cancellation; `Admin/Modules/Index`, `Admin/Modules/Show`, `Admin/Modules/TeamConfiguration`                                                                                             | Shared Module registry and activation service, ModuleGate state, activation history/schedules, shared DataTable exports/saved views                                                                                                                | Owner accepted on 2026-07-30 and directed work to Managed Processes.                                                                                | `php artisan test --filter='ModuleActivationAdministrationTest\|DemoResetTest'`; `pnpm typecheck`; focused ESLint/Prettier; focused Pint on touched PHP files; `pnpm build`.                                                                                                                                                                                                                                                                               | Module activation index is restored as a DataTable with backend-applied filters for category, availability, global/team/effective activation, configuration source, activation support, and pending schedules. Module detail is now module/global-focused, uses Active and Configuration source labels, links team rows to a separate team-configuration screen, and renders non-editable modules as information-only state/dependency/team-table views while editable modules keep supported global activation, global scheduling, cancellation, history, and schedule tables.                                                                                                                                                                                |
| 2026-07-30 | Procesy       | `/admin/managed-processes`, `/admin/managed-processes/{run}`, `/admin/managed-processes/definitions`, `/admin/managed-processes/schedules`, `/admin/managed-processes/schedules/create`, run/retry/cancel/acknowledge/schedule actions; `Admin/ManagedProcesses/Runs`, `Definitions`, `Schedules`, `Schedules/Create`, `Show`, shared `ManagedProcessArea` | Optional ManagedProcesses, process definition registry, managed process runner, process schedules/logs/import execution persistence, process-run acknowledgements, Files-backed manual upload for import starts, shared DataTable exports/saved views | Owner accepted on 2026-07-30 after requested corrections.                                                                                           | `php artisan test --filter='ManagedProcessesAdminTest'`; `php artisan test --filter='FrontendShellTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; focused ESLint/Prettier; `./vendor/bin/pint --dirty --test`.                                                                                                                                                                         | Managed Processes is now the owning Admin workflow surface for combined process and import-linked runs, registered definitions, schedules, modal definition starts, and run timeline/details. Failed, warning, cancelled, and expired runs can be marked as handled through row or bulk actions; handled runs remain in history, can be filtered by handling status, and stop contributing to managed-process warning dashboard signals. The former import execution subview, redirect routes, and Phase 25 demo process definitions were removed; import context remains visible on run detail. Schedule creation is a separate page, and empty selects keep placeholder-only behavior through the shared `FormSelect` primitive. Section links for Runs, Definitions, and Schedules live in `TopBar`/`ShellSubnavigation`, not the sidebar or page-local navigation blocks; breadcrumbs include the same active subsection level, for example `Admin / Procesy / Uruchomienia`.                                                 |
| 2026-07-30 | Kolejki       | `/admin/queues`, `/admin/queues/failed-jobs/retry`, `/admin/queues/failed-jobs/acknowledge`; `Admin/Queues/Index`                                                                                                                                                                                                                              | Shared failed-job persistence, failed-job acknowledgement persistence, queue configuration, queue retry command boundary, security audit, shared DataTable exports/saved views                                                                     | Owner accepted on 2026-07-31 after Managed Processes acknowledgement alignment.                                                                     | `php artisan test --filter='AdminQueuesTest\|FrontendShellTest'`; `php artisan test --filter='AdminQueuesTest\|OperationalAlertsTest'`; `php artisan test --filter='DemoResetTest\|AdminQueuesTest\|ManagedProcessesAdminTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; focused ESLint/Prettier; `./vendor/bin/pint --dirty --test`. | Queues is now a low-level transport diagnostics view rather than a workflow history surface. It shows queue names with failed-job counts, durable failed-job state, backend-applied connection/queue/handling-date filters, stable retry actions, bulk retry, row/bulk mark-as-handled actions, payload/exception detail viewers through `CodeViewer`, and sidebar/mobile route visibility through `admin.queues.index`. Handled failed jobs are hidden by default and stop contributing to Admin failed-job dashboard warnings and operational alert thresholds. Completed/running Atlas-owned work remains in Managed Processes. |
| 2026-07-31 | Pliki         | `/admin/files`, `/admin/files/acknowledge`, `/admin/files/{file}/rescan`; `Admin/Files/Index`                                                                                                                                                                                                                                                    | Core Files private metadata, scan evidence, malware rescan and scan-handling use cases, security audit, shared DataTable exports/saved views                                                                                                      | Owner accepted on 2026-07-31 after requested Files corrections.                                                                                    | `php artisan test --filter='FilesAdminTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `./vendor/bin/pint --dirty --test`.                                                                                                                                                                                                                                                    | Files is now a metadata/status operational view using shared metrics, backend-applied filters for scan state, extension, provider, availability, handling, and created date, shared DataTable pagination/saved views/exports, row/bulk mark-as-handled actions for non-clean scan states, a confirmed row rescan action that reopens handled rows, localized breadcrumbs, and a scan-evidence panel for the currently visible rows. Handled problematic files are hidden by default and stop contributing to Admin file-blocker dashboard signals. The previous bounded newest-files disclaimer pattern and redundant scan-evidence subtitle are removed; the screen does not expose arbitrary filesystem browsing, raw storage paths, physical file manipulation, or manual clean overrides.                                                                                                                                                                      |
| 2026-07-31 | Wyszukiwanie  | `/admin/search`, `/admin/search/rebuild`; `Admin/Search/Index`                                                                                                                                                                                                                                                                                   | Optional Search, Meilisearch readiness, Search index registry, Search rebuild managed process, shared DataTable exports/saved views                                                                                                                | Owner accepted on 2026-07-31 after confirming that rebuild failures are handled in Managed Processes.                                                | `php artisan route:list --path=admin \| rg "imports\|search\|managed-processes"`; `php artisan test --filter='SearchAdminTest\|ManagedProcessesAdminTest\|FrontendShellTest\|ModuleRegistryTest'`; `php artisan test --filter='SearchAdminTest\|DemoResetTest\|ModuleRegistryTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `pnpm exec eslint resources/js/Pages/Admin/Search/Index.vue resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Components/sharedUiGuardrails.test.ts`; `./vendor/bin/pint --dirty --test`. | Search is the Admin operational surface for Meilisearch projections. It shows readiness, index metrics, registered index descriptors in a shared exportable DataTable with module/sensitivity/deletion/anonymization filters, recent rebuild managed-process runs linked to details, and confirmed global or single-index rebuild actions. The readiness card keeps the Meilisearch status without redundant explanatory copy. The old standalone `/admin/imports` route and the redirect-only `/admin/managed-processes/imports` route were removed as stale Phase 25 leftovers; import context remains visible in Procesy. With no business modules installed yet, the index catalog may be empty.                                                                                                                                                                                                 |
| 2026-07-31 | Integracje    | `/admin/integrations`, `/admin/integrations/{integration}/test`; `Admin/Integrations/Index`                                                                                                                                                                                                                                                     | Optional Integrations adapter registry, synchronization history, circuit breakers, external API boundary, shared DataTable exports/saved views                                                                                                     | Owner accepted on 2026-07-31 after confirming the last-error semantics and operational ownership.                                                    | `php artisan test --filter='IntegrationsAdminTest\|IntegrationsModuleTest\|DemoResetTest\|FrontendShellTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `pnpm exec eslint resources/js/Pages/Admin/Integrations/Index.vue resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Components/sharedUiGuardrails.test.ts`; `php artisan route:list --path=admin \| rg "integrations\|search\|files"`; `./vendor/bin/pint --dirty --test`. | Integrations is now a registered Admin sidebar entry again with desktop/mobile navigation, localized breadcrumb, metrics, external API boundary status, backend-applied adapter filters for status/circuit/API/scope, exportable adapter DataTable, permission-protected test-connection row actions, and a compact recent synchronization panel. Concrete adapters and synchronization examples are owned by real modules or tests, not by permanent Phase 25 demo data.                                                                                                                                                                                                                                                                       |
| 2026-07-31 | Flagi funkcji | `/admin/feature-flags`, global/team update and team-clear actions; `Admin/FeatureFlags/Index`                                                                                                                                                                                                                                                    | Optional FeatureFlags registry, selected-team flag evaluation, global/team values, append-only history, shared DataTable exports/saved views                                                                                                       | Owner accepted on 2026-07-31 after confirming the module-level versus feature-level rollout semantics.                                               | `php artisan test --filter='FeatureFlagsAdminTest\|FeatureFlagDefinitionsTest\|DemoResetTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `pnpm exec eslint resources/js/Pages/Admin/FeatureFlags/Index.vue resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Components/sharedUiGuardrails.test.ts`; `php artisan route:list --path=admin \| rg "feature-flags\|integrations\|search\|files"`. | Feature Flags is now a registered Admin sidebar entry with desktop/mobile navigation, localized breadcrumb, summary metrics, selected-team filter, backend-applied status/source/owner/lifecycle filters, exportable flag DataTable, row actions for global value, selected-team override, and selected-team override clearing through a reason-required dialog, plus compact recent history. Concrete flag values and history examples are owned by tests or future modules, not by permanent Phase 25 demo data.                                                                                                                                                                                                                                                                             |
| 2026-07-31 | Limity żądań  | `/admin/rate-limits`, `/admin/rate-limits/reset`; `Admin/RateLimits/Index`                                                                                                                                                                                                                                                                       | Core Identity rate-limit policy catalog, rejection statistics, exact limiter reset boundary, security audit, shared DataTable exports/saved views                                                                                                   | Owner accepted on 2026-07-31.                                                                                                                       | `php artisan test --filter='RateLimitAdministrationTest\|IdentityRateLimitPolicyTest\|DemoResetTest\|FrontendShellTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `pnpm exec eslint resources/js/Pages/Admin/RateLimits/Index.vue resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Components/sharedUiGuardrails.test.ts`; `php artisan route:list --path=admin \| rg "rate-limits\|feature-flags\|files"`. | Rate Limits is now a registered Admin sidebar entry with desktop/mobile navigation, localized breadcrumb, summary metrics, backend-applied filters for policy family, rejection activity, key part, progressive delay, and temporary lock, exportable policy DataTable, and a reason-required dialog for resetting exactly one concrete limiter key. Thresholds remain read-only and no policy update/delete/disable routes exist. Rejection statistics now come only from real runtime activity or explicit test fixtures.                                                                                                                                                                                                                                                   |
| 2026-07-31 | Logi aplikacji | `/admin/logs`; `Admin/Logs/Index`, shared `CodeViewer`                                                                                                                                                                                                                                                                                          | Shared Observability application log reader, sensitive-data redaction, Admin application-log export provider, discovered `storage/logs/*.log` files                                                                                                  | Owner accepted on 2026-07-31 after multi-file and CodeViewer spacing/tooltip corrections.                                                            | `php artisan test --filter='AdminApplicationLogBrowserTest\|ObservabilityFoundationTest\|DemoResetTest\|FrontendShellTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `pnpm exec eslint resources/js/Pages/Admin/Logs/Index.vue resources/js/Components/CodeViewer.vue resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Components/sharedUiGuardrails.test.ts`; `php artisan route:list --path=admin \| rg "logs\|rate-limits\|files\|imports"`; `./vendor/bin/pint --dirty --test`. | Application Logs is now a registered Admin sidebar entry with desktop/mobile navigation, localized breadcrumb, metrics, backend-applied log-file/level/module/source/date/search filters, a split log-list/detail viewer instead of a primary DataTable, safe filtered exports, and an expanded shared `CodeViewer` with line numbers, wrap toggle, copy action, tooltips, breathing room, and log/error highlighting. The backend accepts only discovered `*.log` filenames from `storage/logs`, defaults to `laravel.log` when present, and still rejects path-driven browsing. Demo reset no longer writes synthetic log entries into existing local log files; owner review uses real development/runtime logs.                                                                                                                                                                                      |
| 2026-07-31 | Powiadomienia | `/notifications`, `/notifications/read`, `/notifications/{notification}/read`; `Notifications/Index`, topbar dropdown                                                                                                                    | Core Notifications inbox, realtime event feed, notification localization, notification recipient read state, shared DataTable/saved views                                                                                                          | Owner accepted on 2026-07-31.                                                                                                                       | `php artisan test --filter='NotificationsFoundationTest\|DemoResetTest\|FrontendShellTest'`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Components/Form/formControls.test.ts`; `pnpm exec eslint resources/js/Pages/Notifications/Index.vue resources/js/Components/sharedUiGuardrails.test.ts`; `php artisan route:list --path=notifications`; `./vendor/bin/pint --dirty --test`. | Notifications remains the regular application inbox surface, not a duplicate Admin diagnostics route. The center now uses `PageStack`, operational metrics, backend-applied status/severity/scope/type/link/date filters, localized scope/severity labels, DataTable saved views, row and bulk mark-as-read actions, and deep-link opening for linked notifications. Admin DataTable exports are intentionally not exposed from `/notifications` because the existing export endpoint is Admin-scoped; terminal export/process notifications remain visible through the inbox and topbar dropdown without adding another export action. Representative notification records are owned by tests or real module workflows, not by permanent Phase 25 demo data. |
| 2026-07-31 | Audit / security | `/admin/audit`, `/admin/audit/security-history`, `/admin/audit/impersonation/{session}`; `Admin/Audit/Index`, `Admin/Audit/SecurityHistory`, `Admin/Audit/ImpersonationSession` | Core Audit append-only event store, security audit projection, impersonation audit context, shared DataTable exports/saved views                                                                                                                     | Owner accepted on 2026-07-31 after audit/security distinction and impersonation banner corrections.                                                 | `php artisan test --filter='AuditFoundationTest\|DemoResetTest\|FrontendShellTest'`; `php artisan test --filter=AdminModeImpersonationTest`; `php artisan test --filter=LocalizationTest`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts`; `pnpm exec eslint resources/js/Pages/Admin/Audit/Index.vue resources/js/Pages/Admin/Audit/SecurityHistory.vue resources/js/Pages/Admin/Audit/ImpersonationSession.vue resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Layouts/AppLayout.vue resources/js/Components/sharedUiGuardrails.test.ts`; `php artisan route:list --path=admin/audit`. | Audit / Security is now a registered Admin sidebar entry with desktop/mobile navigation, localized breadcrumbs, top subnavigation between events and security history, metrics, backend-applied filters, saved views, exports, and a session detail view for impersonation sessions. The surface remains read-only because audit records are immutable diagnostic evidence, not operational incidents to acknowledge or resolve. The global impersonation banner now uses localized PL/EN copy and resolves team display names instead of technical team names. |
| 2026-07-31 | Linki diagnostyczne | `/admin/pulse`, `/telescope`; package-owned Pulse and Telescope dashboards                                                                                                                                                                                                                                                                    | Laravel Pulse and Telescope package routes, Admin permission exposure, active-team authorization gates, external navigation handling                                                                                                                  | Owner accepted on 2026-07-31.                                                                                                                       | `php artisan route:list --path=admin/pulse`; `php artisan route:list --path=telescope`; `php artisan test --filter=FrontendShellTest`; `pnpm typecheck`; `pnpm build`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts`; `pnpm exec eslint resources/js/Components/Sidebar.vue resources/js/Components/MobileNavigation.vue resources/js/Components/sharedUiGuardrails.test.ts`; `git diff --check`. | Pulse and Telescope are restored as external Admin navigation links under Diagnostics rather than rebuilt Atlas Inertia views. Pulse is offered when the package route exists and the operator has `admin.pulse.view`. Telescope is offered only in `local` or `development` environments when the package route exists and the operator has `admin.telescope.view`. Desktop and mobile navigation open both package dashboards outside the Inertia shell with `noopener noreferrer`. |
| 2026-07-31 | Flash/toasty  | Admin request-result flashes, managed-process terminal notifications, realtime event handling; shared `ToastViewport`/`useToast`                                                                                                                                                                                                              | Shared flash/toast discipline, Core Notifications realtime feed, Managed Processes terminal notification publisher                                                                      | Applied as Phase-25-wide guardrail.                                                                                                                  | `php artisan test --filter='FlashMessageDisciplineTest\|ManagedProcessesAdminTest\|ReportsModuleTest\|NotificationsFoundationTest\|FrontendShellTest'`; `pnpm exec vitest run resources/js/Components/sharedUiGuardrails.test.ts resources/js/Services/networkHandling.test.ts`; focused ESLint; focused Pint. | One user action now has an automated guardrail for at most one immediate Admin flash message. Admin flashes must use translation keys, managed-process terminal notifications store localized keys and safe human parameters, and realtime `operation.progress` events no longer create visible toast stacks. Technical values stay in process details, tables, logs, audit records, or diagnostic screens. |
| 2026-07-31 | Browser review guardrails | `ToastViewport`, Users account actions, Playwright Admin coverage and snapshots                                                                                                                                                                                                                                                             | Shared toast viewport interaction layer, Admin user account actions, browser-level regression suite                                                                                    | Applied as Phase-25-wide guardrail after full Playwright review.                                                                                    | `pnpm build`; `pnpm exec playwright test tests/e2e/frontend-theme.spec.ts -g "renders the admin shell" --update-snapshots`; `pnpm exec playwright test tests/e2e/admin-impersonation.spec.ts`; `pnpm test:e2e`; focused ESLint; `pnpm typecheck`. | Toast notifications no longer intercept pointer events outside their close button, so transient flashes cannot block form actions. The Users table/edit shared account-action composable now hides `Impersonuj` when backend eligibility says impersonation cannot start, including self, administrator, technical, inactive, or unavailable-team targets. Playwright selectors were updated for accepted Polish/Admin copy and refreshed Admin shell snapshots. |
| 2026-07-23 | Review data   | `Database\Seeders\DevelopmentDemoSeeder`; `composer demo:reset`; removed `app/Modules/Application/Demo`                                                                                                                                                                                                                                       | Development-only seeding and placeholder module for Phase 25 owner review                                                                                                                                                                          | Cleanup completed on 2026-07-31 after owner confirmed review data must not become permanent demo scope.                                             | `php artisan test --filter='DemoResetTest\|ModuleActivationAdministrationTest\|ManagedProcessesAdminTest\|SearchAdminTest\|ModuleRegistryTest'`; `php artisan route:list --path=admin \| rg "imports\|managed-processes\|search\|modules"`.                                                                                                                                                                                                                                                                           | The temporary Phase 25 review dataset was removed. `DevelopmentDemoSeeder` is now an empty/no-op seeder, `app/Modules/Application/Demo` is gone, the deployed module registry no longer includes `DemoModule`, Search no longer receives demo index descriptors, Managed Processes no longer exposes demo no-op definitions, Notifications no longer carries `notifications.demo.*` copy, and leftover import-subview translation/routing traces were removed. `composer demo:reset` still creates the local preview administrator through `DevelopmentBootstrapSeeder`, then returns an otherwise clean foundation system. |

## Phase 25 Workflow-Module Extraction Audit

This audit records rebuilt or partially rebuilt surfaces that must be reconsidered as coherent workflow modules instead of page-local compositions before the relevant sidebar entry is accepted.

| Date       | Area                                                             | Current shape                                                                                                                                                                                                                                                                                                                                                                                      | Required extraction or decision                                                                                                                                                                                                                                                                                           | Status                                             |
| ---------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| 2026-07-23 | Users Team access create/edit                                    | `Admin/Users/Create.vue` and `Admin/Users/Edit.vue` now compose `resources/js/Components/Users/UserTeamAccessWorkflow.vue` instead of rebuilding the Team access card, add-team selector, assignment source selector, searchable counted role/direct-permission controls, effective preview, and action buttons page-locally.                                                                      | Keep one shared Team access workflow module with create/edit modes and typed props/events. It must make create and edit visually and behaviorally identical wherever the task is the same, and mode differences must remain explicit in the component contract.                                                           | Implemented; pending focused owner browser review. |
| 2026-07-23 | Users account actions index/edit                                 | `Admin/Users/Index.vue` and `Admin/Users/Edit.vue` now share `resources/js/Composables/useAdminUserAccountActions.ts` for action labels, disabled reasons, route suffixes, tones, table actions, bulk actions, and edit record actions.                                                                                                                                                            | Keep row, bulk, and record account actions backed by the shared composable or a later backend-provided action contract while preserving state-specific disabled reasons and reactive localization.                                                                                                                        | Implemented; pending focused owner browser review. |
| 2026-07-23 | Users account sensitivity labels/options                         | Sensitivity display/options are now mapped through `resources/js/Composables/useAccountSensitivityOptions.ts` for table rows and edit selects.                                                                                                                                                                                                                                                     | Keep account sensitivity labels/options in a shared formatter/options provider so table, edit form, exports-adjacent UI, and future filters use one localized source.                                                                                                                                                     | Implemented; pending focused owner browser review. |
| 2026-07-23 | Teams user access create/edit                                    | `Admin/Teams/Create.vue` and `Admin/Teams/Edit.vue` compose `resources/js/Components/Teams/TeamMemberAccessWorkflow.vue` for user selection, role checklist, permission checklist, effective preview, edit reasons, and remove/save action placement from the team side.                                                                                                                           | Keep team-side user access in one workflow component and keep it visually aligned with Users Team access while preserving the opposite-side route and payload contract.                                                                                                                                                   | Implemented; pending focused owner browser review. |
| 2026-07-23 | Future Roles/Permissions/Onboarding Packages assignment surfaces | These areas are likely to expose similar role/permission selection, preset, preview, and create/edit paired forms.                                                                                                                                                                                                                                                                                 | Reuse the authorization assignment controls/preview extracted from Users or create a lower-level authorization-assignment workflow module used by Team access and package editing. Searchable counted role/permission checklists must use the shared `SearchableCheckboxList` primitive.                                  | Carry forward to Authorization rebuild.            |
| 2026-07-23 | Roles create/edit permission bundle                              | `Admin/Authorization/Roles/Create.vue` and `Edit.vue` compose `resources/js/Components/Authorization/RoleForm.vue` for role identity, permission search, permission checklist, selected-count preview, and action placement.                                                                                                                                                                       | Keep create/edit role permission-bundle editing in one workflow component. If Onboarding Packages needs a similar permission bundle surface, reuse the shared `SearchableCheckboxList` portion instead of copying it locally.                                                                                             | Implemented; pending owner browser review.         |
| 2026-07-23 | Szablony create/edit onboarding bundle                           | `Admin/Authorization/Packages/Create.vue` and `Edit.vue` compose `resources/js/Components/Authorization/OnboardingPackageForm.vue` for team context, template identity, role checklist, permission checklist, selected-count preview, and action placement.                                                                                                                                        | Keep create/edit template editing in one workflow component. Szablony remain team-scoped onboarding templates and must reuse searchable counted assignment controls instead of page-local role/permission list composition.                                                                                               | Implemented; pending owner browser review.         |
| 2026-07-23 | Manager hierarchy and relationship editor                        | `Admin/Managers/Create.vue` and `Admin/Managers/Edit.vue` expose the same accepted manager control sequence: selected manager context, hierarchy tree, bulk direct-report assignment, impact preview, head-manager update, and inline direct-report termination. Both pages compose `ManagerHierarchyTree.vue` plus recursive `ManagerHierarchyTreeNode.vue` for the selected manager's hierarchy. | Keep the create/edit manager workflow visually and behaviorally paired because adding a manager and adding reports under an existing manager share the same fields, preview, validation, and footer actions. Keep the hierarchy visualization shared because manager subtrees will be reused by later manager workspaces. | Implemented; pending owner browser review.         |
